# 🟨 T-Plexity - Интеллектуальная система для работы с инвестиционными новостями

**T-Plexity** — интеллектуальная система, которая в реальном времени отслеживает и агрегирует публикации из проверенных инвестиционных Telegram-каналов. Работает на актуальных данных с минимальной задержкой и формирует точные, контекстные ответы на вопросы о финансовых рынках.

- **Источники** — верифицированные новостные и инвестиционные Telegram-каналы  
- **Актуальность** — информация появляется в сервисе мгновенно после публикации  
- **Ответы** — генерируются LLM — лаконичные, точные, с рыночным контекстом  
- **Прозрачность** — каждый тезис ответа сопровождается ссылкой на источник

## 💡 Гипотезы

- **Информационный шум** — пользователи тонут в потоке сообщений и тратят время на фильтрацию
- **Запаздывание информации** — без автоматизации легко пропустить инвестиционную возможность
- **Проблема доверия и верификации** — без ссылки на канал инвестор не готов действовать
- **Недостаток персонализации** — важен собственный шорт-лист источников под портфель

## 📊 Результаты интервью

- **Информационная перегрузка** — 8/9 респондентов признают, что пропускают важные новости из-за объема данных
- **Проблема доверия** — 7/9 не совершают действие, пока не увидят первоисточник
- **Запаздывание информации** — 7/9 привели примеры упущенной выгоды из-за ручного мониторинга
- **Персонализация** — 6/9 готовы читать любой релевантный канал, если он покрывает их интересы — фокус сместился на полезность, а не на бренд канала

## 📈 Дерево метрик

- **Продуктовая ценность**: DAU/WAU/MAU, Sessions per user, Feature usage, Return rate (D1/D7/D30)
- **Монетизация**: Paywall CTR, конверсия Free → Trial → Paid, Churn rate, MRR
- **Рост и масштабирование**: New users per day, CAC, LTV/CAC, Gross margin

## 🏗️ Архитектура системы

Система построена на микросервисной архитектуре и состоит из следующих компонентов:

### 1. Parser Service (порт 8011)

- Мониторинг каналов в реальном времени
- Определение актуальности новостей — LLM и правила определяют срок актуальности и автоматически удаляют устаревшие публикации
- Автоматическая индексация в поисковую систему

### 2. Retriever Service (порт 8010)

- Семантический поиск — понимание смысла запроса
- Лексический поиск — точное совпадение ключевых слов
- Ранжирование — улучшение качества результатов поиска
- Поддержка русского языка с лемматизацией

### 3. Generation Service (порт 8012)

- Переформулирование запросов с учетом контекста диалога
- Итеративное улучшение ответов (ReAct подход)
- Оценка релевантности найденных документов
- Генерация ответов с цитированием источников

### 4. LLM Client (порт 8014)

- Поддержка различных провайдеров (OpenAI, OpenRouter)
- Оптимизация запросов через батчинг
- Надежная обработка ошибок

### 5. Bot Service (порт 8013)

- Интуитивный интерфейс
- История диалога с возможностью очистки
- Форматирование ответов с источниками
- Опция генерации краткого ответа

### Внешние сервисы

- Qdrant (порты 6333/6334) — векторная база данных для хранения эмбеддингов
- Redis (порт 6389) — хранилище для истории диалогов

## ⚙️ Настройка и запуск

### Требования

- Python >= 3.11 и Poetry (для локальной разработки)
- Docker и Docker Compose (для запуска через контейнеры)

### 🧩 Настройка окружения

Создайте файлы `.env` для каждого сервиса на основе `example.env`:

```bash
cp src/tplexity/retriever/example.env src/tplexity/retriever/.env
cp src/tplexity/parser/example.env src/tplexity/parser/.env
cp src/tplexity/generation/example.env src/tplexity/generation/.env
cp src/tplexity/bot/example.env src/tplexity/bot/.env
cp src/tplexity/llm_client/example.env src/tplexity/llm_client/.env
```

Заполните переменные окружения: API ключи, настройки Telegram и другие параметры.

### 🚀 Запуск сервисов

#### 🐳 Через Docker Compose

```bash
docker-compose up --build
```

Все сервисы будут доступны на соответствующих портах (см. раздел "Архитектура системы").

#### 💻 Локальная разработка

Для локальной разработки установите зависимости и запустите сервисы:

```bash
# 1. Установка зависимостей
poetry install

# 2. Запуск Qdrant и Redis (в отдельных терминалах)

docker run -p 6333:6333 -p 6334:6334 qdrant/qdrant
docker run -p 6389:6379 redis

# 3. Запуск сервисов tplexity (в отдельных терминалах)
poetry run uvicorn tplexity.retriever.app:app --port 8010
poetry run uvicorn tplexity.parser.app:app --port 8011
poetry run uvicorn tplexity.generation.app:app --port 8012
poetry run uvicorn tplexity.bot.app:app --port 8013
poetry run uvicorn tplexity.llm_client.app:app --port 8014
```

## 📊 Оценка качества

### Retrieval Evaluation

Лучшая конфигурация `Frida + BM25 + Lemma` показала следующие результаты:

- **HitRate@5 = 0.6706** и **HitRate@10 = 0.7328** — вероятность, что хотя бы один релевантный документ окажется в топ-K
- **MRR@5 = 0.5212** и **MRR@10 = 0.5376** — усредненный обратный ранг первой релевантной находки

### Generation Evaluation

- **Relevance: 9.1/10** ⭐ — измеряет соответствие ответа поставленному вопросу (насколько ответ релевантен запросу пользователя)
- **Completeness: 9.1/10** ⭐ — оценивает полноту ответа (учтены ли все ключевые аспекты вопроса, есть ли пропуски)
- **Faithfulness: 9.2/10** ⭐ — отражает достоверность и корректность ответа (нет ли искажений, галлюцинаций или ошибок фактов)
- **Off-topic rate: 0.1** — доля ответов, которые отклоняются от темы, требуют уточнения или дополнительного поиска (только 10% ответов оказались нерелевантны или недостаточно информативны)

### Routing Evaluation

- **Accuracy: 0.71** — общая точность классификации запросов на требующие и не требующие RAG
- **Precision: 0.63** — доля корректных решений Router о необходимости RAG среди всех случаев, когда Router решил использовать RAG
- **Recall: 0.99** 🎯 — доля корректных решений Router о необходимости RAG среди всех запросов, действительно требующих RAG. Подтверждает, что Router не пропускает запросы, требующие поиска в базе знаний, и не создает узких мест в пайплайне.
- **F1: 0.78** — гармоническое среднее precision и recall для балансировки метрик

## 📁 Структура проекта

```text
tplexity/
├── src/tplexity/
│   ├── bot/              # Telegram бот сервис
│   ├── generation/       # Генерация ответов с RAG
│   ├── llm_client/      # Клиент для работы с LLM
│   ├── parser/           # Парсинг Telegram каналов
│   └── retriever/        # Гибридный поисковый движок
├── evaluation/           # Оценка качества системы
│   ├── generation/       # Оценка генерации
│   ├── retrieval/        # Оценка поиска
│   └── routing/          # Оценка роутинга
├── tests/                # Тесты
├── docs/                 # Артефакты исследования
├── presentation.pdf      # Презентация проекта
├── docker-compose.yml    # Оркестрация сервисов
└── pyproject.toml        # Зависимости проекта
```
